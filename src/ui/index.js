/**
 * UI Adapter Layer
 * 
 * Change the import path below to swap UI implementations.
 * Available options:
 * - './implementations/mantine' - Mantine UI (modern, polished)
 * - './implementations/mui' - Material-UI (Google Material Design)
 * - './implementations/tailwind' - Tailwind CSS (utility-first)
 */

// Change this line to swap UI implementations
export * from './implementations/mantine';

// Examples:
// export * from './implementations/mui';
// export * from './implementations/tailwind';
